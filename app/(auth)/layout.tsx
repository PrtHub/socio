const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <main className="w-full h-screen flex items-center justify-center bg-[#313338]">{children}</main>;
};

export default AuthLayout;
